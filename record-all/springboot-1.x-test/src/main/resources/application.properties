debug=false
spring.application.name=user-service

eureka.client.service-url.defaultZone: http://krista.service.registry.B:18765/eureka/

# 关闭默认favicon
# spring.mvc.favicon.enabled=true
server.port=8000

security.basic.enabled=false
security.user.name=krista
security.user.password=krista008
management.security.enabled=true
management.security.role=ADMIN

# 开启/shutdown,默认false
endpoints.shutdown.enabled=false

# 定义系统的info信息
info.applicationName=springboot-1.x-test
info.author.name=krista
info.author.email=1366678737@qq.com

# database
spring.jpa.generate-ddl=false
spring.jpa.show-sql=true
spring.jpa.hibernate.ddl-auto=none
spring.datasource.platform=h2
spring.datasource.schema=classpath:schema.sql
spring.datasource.data=classpath:data.sql

logging.level.root=info
logging.level.org.hibernate:info
logging.level.org.hibernate.type.descriptor.sql.BasicBinder:trace
logging.level.org.hibernate.type.descriptor.sql.BasicExtractor:trace